<% var value; %>
<% if (item.value == 'user_id'){ %>
    <% value = user._id %>
<% } %>

<%
    var new_prefix = 'new.'

    if (typeof obj_prefix == 'undefined' || obj_prefix == false){
        obj_prefix = ''
    } else {
        obj_prefix = obj_prefix+'.'
    }

    if (typeof array_obj != 'undefined'){
        if (array_obj === true){
            new_prefix = ''
        } else if (typeof array_obj == 'string'){
            new_prefix = 'new.'+array_obj+'.'
        }
    }
%>

<input type="hidden" app-model="<%- new_prefix %><%- obj_prefix %><%- item.name %>" value="<%- value %>">
