<% layout(config.site.theme+'/fullscreen.ejs') -%>

<div class="page grid col-2 col-s-1">

    <div class="flex flex-middle-left flex-column px-9 px-s-2">

        <div class="flex-none grid col-2 w-100 mt-4">

            <div>
                <a href="/" class="block w-20"><% if (typeof view != 'undefined' && view.logo){ %><img src="<%- view.logo %>"><% } %></a>
            </div>
            <div class="flex flex-middle-right">
                <% if (view.users.allow_registration === true){ %>
                    <a href="/login">Log In</a>
                <% } %>
            </div>

        </div>

        <div class="flex-5 flex flex-column flex-middle-left w-100 mb-4">

            <h1 class="text-light mb-2">Sign Up</h1>

            <% if (typeof error != 'undefined'){ %>
                <div class="mb-2 text-33-red">
                    <%- error %>
                </div>
            <% } %>

            <form method="post" action="/login/register" class="grid col-1 col-s-1 gap-1 w-100">

                <input type="text" name="email" placeholder="Email Address" class="underline">


                <input type="password" name="password" placeholder="Password" class="underline">
                <input type="password" name="password_conf" placeholder="Confirm Password" class="underline">

                <input type="submit" class="mt-2 btn highlight thick w-100" value="Sign Up!">

                <div class="text-center">
                    <a class="" href="/login/<%- guard %>">Have an account? Login</a>
                </div>

            </form>

        </div>

    </div>

    <div class="bg-33-dream flex flex-middle-left flex-column p-6 p-s-2">



    </div>

</div>
